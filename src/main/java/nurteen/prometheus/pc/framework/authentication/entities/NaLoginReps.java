package nurteen.prometheus.pc.framework.authentication.entities;

public class NaLoginReps extends LoginReps {
    public NaLoginReps(String nuid, String ndid, String accessToken, String nickname, String headimg) {
        super(nuid, ndid, accessToken, nickname, headimg);
    }
}
