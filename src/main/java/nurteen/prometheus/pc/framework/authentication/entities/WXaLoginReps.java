package nurteen.prometheus.pc.framework.authentication.entities;

public class WXaLoginReps extends LoginReps {
    public WXaLoginReps(String nuid, String ndid, String accessToken, String nickname, String headimg) {
        super(nuid, ndid, accessToken, nickname, headimg);
    }
}
